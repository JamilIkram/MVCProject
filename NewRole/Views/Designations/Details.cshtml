@model NewRole.Models.DataBind.Designation

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var listTblUserRole = (List<NewRole.Models.DataBind.TblUserRole>)Session["TblUserRoles"];
}

<h2>Details</h2>

<div>
    <h4>Designation</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DesignationName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DesignationName)
        </dd>

    </dl>
</div>
<p>
    @if (listTblUserRole.Where(o => o.PageName == "Depertments" && o.IsUpdate == true).FirstOrDefault() != null)
    {
        <a href="/Customers/Edit?id=@Model.DesignationId">Edit</a>
    }
    |
    @Html.ActionLink("Back to List", "Index")
</p>

